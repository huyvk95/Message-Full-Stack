import { StyleSheet } from "react-native";
import baseStyle from "./base";

const app = StyleSheet.create({
    wrap: {
        flex: 1,
    },
    card: {
        backgroundColor: "#fff"
    },
})

export default app